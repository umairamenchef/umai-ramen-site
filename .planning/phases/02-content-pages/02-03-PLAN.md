---
phase: 02-content-pages
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/[locale]/menu/page.tsx
  - src/components/menu/MenuStickyNav.tsx
  - src/components/menu/MenuCategory.tsx
  - src/components/menu/MenuItem.tsx
  - src/components/menu/DietaryBadge.tsx
  - src/components/menu/ExtrasGrid.tsx
  - src/components/menu/FormulesSection.tsx
autonomous: true
requirements:
  - MENU-01
  - MENU-02
  - MENU-03
  - MENU-07
  - INTG-04

must_haves:
  truths:
    - "Menu page displays all categories and items from Sanity with correct locale text"
    - "Sticky sub-navigation highlights the currently visible category"
    - "Each menu item shows FR name, JP name, price, description, and dietary badges"
    - "Extras section displays as a compact grid with name and price"
    - "Formules section shows included items in a highlighted dotted-border block"
    - "Items with available=false in Sanity do not appear (MENU-06 filter in GROQ)"
    - "eazee-link CTA appears at bottom of menu page"
  artifacts:
    - path: "src/app/[locale]/menu/page.tsx"
      provides: "Menu page route with Sanity data fetching"
      contains: "MENU_CATEGORIES_QUERY"
    - path: "src/components/menu/MenuStickyNav.tsx"
      provides: "Sticky category sub-navigation"
      contains: "IntersectionObserver"
    - path: "src/components/menu/MenuItem.tsx"
      provides: "Individual menu item row"
      contains: "DietaryBadge"
    - path: "src/components/menu/ExtrasGrid.tsx"
      provides: "Extras compact grid"
      contains: "MENU_EXTRAS"
    - path: "src/components/menu/FormulesSection.tsx"
      provides: "Formules highlighted section"
      contains: "includedItems"
  key_links:
    - from: "src/app/[locale]/menu/page.tsx"
      to: "src/sanity/lib/queries.ts"
      via: "sanityFetch with MENU_CATEGORIES_QUERY + MENU_EXTRAS_QUERY + MENU_FORMULES_QUERY"
      pattern: "MENU_CATEGORIES_QUERY"
    - from: "src/components/menu/MenuStickyNav.tsx"
      to: "src/components/menu/MenuCategory.tsx"
      via: "IntersectionObserver targeting category section IDs"
      pattern: "category-.*slug"
    - from: "src/components/menu/MenuItem.tsx"
      to: "src/lib/localized.ts"
      via: "localized() for name, description"
      pattern: "localized"
---

<objective>
Build the complete menu page with Sanity-driven categories, items with dietary badges, sticky sub-navigation, extras grid, formules section, and eazee-link CTA.

Purpose: The menu is the core content page — visitors need to browse ramen categories, see prices and dietary info, and find extras/formules. The sticky nav enables quick navigation across a long page.
Output: A fully functional menu page at /[locale]/menu with all menu data from Sanity.
</objective>

<execution_context>
@/home/ekitcho/.claude/get-shit-done/workflows/execute-plan.md
@/home/ekitcho/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-pages/02-RESEARCH.md
@.planning/phases/02-content-pages/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build MenuStickyNav, MenuItem, DietaryBadge, and MenuCategory components</name>
  <files>
    src/components/menu/MenuStickyNav.tsx
    src/components/menu/MenuCategory.tsx
    src/components/menu/MenuItem.tsx
    src/components/menu/DietaryBadge.tsx
  </files>
  <action>
    1. Create `src/components/menu/DietaryBadge.tsx` — small pill badges:
       - Props: `type: 'vegetarian' | 'gluten-free'`
       - Vegetarian: `bg-green-100 text-green-800` pill with text "V" or "Vegetarien"
       - Gluten-free: `bg-amber-100 text-amber-800` pill with text "SG" or "Sans Gluten"
       - Styling: `inline-flex items-center px-2 py-0.5 text-[10px] font-body font-medium uppercase tracking-wider rounded-full`

    2. Create `src/components/menu/MenuItem.tsx` — individual menu item row:
       - Props: `name: localeString, nameJp?: string, price: number, description?: localeText, isVegetarian: boolean, isGlutenFree: boolean, locale: string`
       - Layout: `flex items-start gap-4 py-5 border-b border-umai-line last:border-0`
       - Left side (`flex-1`):
         - Name row: `div` with `flex items-center gap-2 flex-wrap`
           - `h3 font-display text-xl uppercase tracking-wide` with `localized(name, locale)`
           - JP name: `span font-jp text-xs text-umai-accent opacity-70` (if nameJp exists)
           - `DietaryBadge type="vegetarian"` if isVegetarian
           - `DietaryBadge type="gluten-free"` if isGlutenFree
         - Description: `p font-body text-sm text-umai-text-muted mt-1` with `localized(description, locale)` (if exists)
       - Right side: `span font-body text-base font-medium tabular-nums shrink-0` with price formatted as `{price.toFixed(2).replace('.', ',')} EUR` — use the euro sign character
       - Import `localized` from `@/lib/localized`

    3. Create `src/components/menu/MenuCategory.tsx` — category section:
       - Props: `category` object (from MENU_CATEGORIES_QUERY result), `locale: string`
       - Section with `id={`category-${category.slug.current}`}` — this ID is what MenuStickyNav targets
       - `SectionHeader` with localized category name and optional JP label
       - Map over `category.items` rendering `MenuItem` for each
       - If no items in category (all filtered by available=false), don't render the section

    4. Create `src/components/menu/MenuStickyNav.tsx` — sticky sub-nav:
       - `'use client'` component (uses IntersectionObserver + useState)
       - Props: `categories: Array<{ slug: { current: string }; name: { fr: string; en?: string; de?: string } }>`, `locale: string`
       - Sticky positioning: `sticky top-20 z-30 bg-umai-bg border-b border-umai-line overflow-x-auto` — `top-20` accounts for 80px header
       - Horizontal scrollable list of category names as buttons
       - `useEffect` with `IntersectionObserver` on each `category-{slug}` section element:
         - Use `threshold: [0, 0.25, 0.5, 0.75, 1]` and `rootMargin: '-20% 0px -60% 0px'`
         - Track which section has highest `intersectionRatio`
         - Set active slug to the highest-visibility section
       - Active button: `border-b-2 border-umai-accent text-umai-accent`
       - Inactive button: `border-b-2 border-transparent text-umai-text-muted hover:text-umai-text`
       - Each button: `px-5 py-4 font-body text-xs uppercase tracking-widest whitespace-nowrap transition-colors`
       - onClick: `scrollIntoView({ behavior: 'smooth', block: 'start' })` — the CSS `scroll-padding-top: 132px` added in Plan 01 handles the offset
       - Cleanup: disconnect all observers on unmount
       - Use `localized(cat.name, locale)` for category name display
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - All four component files exist with named exports
    - MenuStickyNav.tsx has `'use client'` directive
    - MenuItem.tsx imports `localized` from `@/lib/localized`
    - DietaryBadge supports both 'vegetarian' and 'gluten-free' types
  </verify>
  <done>
    Core menu components ready: DietaryBadge renders pill tags, MenuItem displays name/JP/price/description/badges, MenuCategory renders a section with items, MenuStickyNav tracks visible section via IntersectionObserver.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build ExtrasGrid, FormulesSection, and assemble the menu page</name>
  <files>
    src/components/menu/ExtrasGrid.tsx
    src/components/menu/FormulesSection.tsx
    src/app/[locale]/menu/page.tsx
  </files>
  <action>
    1. Create `src/components/menu/ExtrasGrid.tsx` — compact extras grid:
       - Props: `extras` array from MENU_EXTRAS_QUERY, `locale: string`
       - `SectionHeader` with title from i18n `menu.extrasTitle`
       - Grid: `grid grid-cols-2 md:grid-cols-3 gap-4`
       - Each extra: `div` with `bg-white/50 p-4 text-center border border-umai-line`
         - `span font-body text-sm font-medium` with `localized(extra.name, locale)`
         - `span font-body text-sm text-umai-text-muted block mt-1` with price formatted as EUR
       - No descriptions needed per locked decision
       - Only renders if extras array is non-empty

    2. Create `src/components/menu/FormulesSection.tsx` — highlighted formules:
       - Props: `formules` array from MENU_FORMULES_QUERY, `locale: string`, `includesLabel: string` (i18n for "includes")
       - Section with `SectionHeader` title from i18n `menu.formulesTitle`
       - Each formule: `div` with `border border-dashed border-umai-accent p-8 mb-6` (dotted border per locked decision)
         - Header row: `flex justify-between items-center`
           - `h3 font-display text-2xl uppercase tracking-wide` with `localized(formule.name, locale)`
           - `span font-body text-lg font-medium tabular-nums` with price
         - Description: `p font-body text-sm text-umai-text-muted mt-2` if exists
         - Included items: `ul mt-4` with each item as `li font-body text-sm` prefixed with a bullet or check mark
       - Only renders if formules array is non-empty

    3. Create `src/app/[locale]/menu/page.tsx` — full menu page:
       - Async server component following established pattern:
         ```typescript
         const { locale } = await params;
         setRequestLocale(locale);
         const t = await getTranslations({ locale, namespace: 'menu' });
         ```
       - Fetch three queries in parallel:
         ```typescript
         const [categories, extras, formules] = await Promise.all([
           sanityFetch({ query: MENU_CATEGORIES_QUERY, tags: ['menuCategory', 'menuItem'] }),
           sanityFetch({ query: MENU_EXTRAS_QUERY, tags: ['menuExtra'] }),
           sanityFetch({ query: MENU_FORMULES_QUERY, tags: ['menuFormule'] }),
         ]);
         ```
       - Also fetch siteSettings for eazeeLinkUrl:
         ```typescript
         const settings = await sanityFetch({ query: SITE_SETTINGS_QUERY, tags: ['siteSettings'] });
         ```
       - Page structure:
         1. `SectionHeader` with `t('title')` as page title
         2. `MenuStickyNav` with categories and locale (passes only slugs/names needed for navigation)
         3. Map over categories rendering `MenuCategory` for each, wrapped in max-width container
         4. `ExtrasGrid` section (if extras exist)
         5. `FormulesSection` section (if formules exist)
         6. eazee-link CTA at bottom: `Button variant="outline"` linking to `settings.eazeeLinkUrl` with `external` prop, text from `t('eazeeLinkCta')`
       - Wrap the main content in `max-w-[var(--max-width-content)] mx-auto px-6 lg:px-10`
       - Add `py-[var(--spacing-section)]` for top/bottom spacing
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `npx next build` succeeds — /[locale]/menu routes are static
    - Visit `http://localhost:3000/fr/menu` — categories display with items, sticky nav works
    - Scroll: sticky nav highlights change as sections enter viewport
    - Click category in sticky nav: page scrolls to correct section, heading visible below sticky elements
    - eazee-link CTA at bottom links externally
    - Extras section renders as a compact grid (if extras exist in Sanity)
    - Formules section renders with dotted borders (if formules exist in Sanity)
  </verify>
  <done>
    Complete menu page at /[locale]/menu displays all Sanity categories with items, dietary badges, JP names, and prices. Sticky sub-nav tracks and scrolls to category sections. Extras grid and formules section render when data exists. eazee-link CTA links to external digital menu.
  </done>
</task>

</tasks>

<verification>
- `npx next build` — menu route static for all 3 locales
- Categories render with items from Sanity, filtered by available==true
- Sticky nav tracks scroll position and allows clicking to jump to sections
- Items show: localized name, JP name (if set), price in EUR format, description, dietary badges
- Extras grid shows compact name+price cards
- Formules section shows dotted-border cards with included items
- eazee-link CTA at bottom opens external URL
- All text renders correctly in /fr, /en, /de
</verification>

<success_criteria>
The menu page is fully functional with Sanity-driven content. A visitor can browse categories via sticky nav, see all items with prices and dietary info, view extras and formules sections, and access the eazee-link digital menu. Items hidden in Sanity Studio disappear without code changes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-pages/02-03-SUMMARY.md`
</output>
