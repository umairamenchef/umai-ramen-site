---
phase: 02-content-pages
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/[locale]/reservation/page.tsx
  - src/app/[locale]/commander/page.tsx
  - src/app/[locale]/notre-histoire/page.tsx
  - src/app/[locale]/infos/page.tsx
  - src/app/[locale]/galerie/page.tsx
  - src/components/gallery/GalleryGrid.tsx
  - src/components/gallery/GalleryLightbox.tsx
autonomous: true
requirements:
  - RESV-01
  - RESV-02
  - RESV-03
  - ORDR-01
  - ORDR-02
  - ORDR-03
  - ORDR-04
  - HIST-01
  - HIST-02
  - HIST-03
  - HIST-04
  - INFO-01
  - INFO-02
  - INFO-03
  - INFO-04
  - INFO-05
  - GLRY-01
  - GLRY-02
  - GLRY-03
  - INTG-03
  - INTG-05
  - INTG-07

must_haves:
  truths:
    - "Reservation page has prominent Gusty CTA with micro-copy about free/no commission"
    - "Reservation page shows address, hours, phone, and Google Maps embed"
    - "Commander page shows 3 ordering options as cards: Uber Eats, Click & Collect, eazee-link"
    - "Notre Histoire has 4 scroll sections with alternating photo-text layout and fade-in animations"
    - "Notre Histoire ends with a reservation CTA"
    - "Infos page displays structured opening hours, address, phone, Google Maps, and FAQ"
    - "Gallery shows responsive photo grid with clickable lightbox"
    - "Lightbox supports keyboard navigation (arrow keys, Escape) and mobile swipe"
    - "Google Maps embed is lazy-loaded on reservation and infos pages"
  artifacts:
    - path: "src/app/[locale]/reservation/page.tsx"
      provides: "Reservation page with Gusty CTA"
      contains: "reservationUrl"
    - path: "src/app/[locale]/commander/page.tsx"
      provides: "Commander page with 3 ordering option cards"
      contains: "uberEatsUrl"
    - path: "src/app/[locale]/notre-histoire/page.tsx"
      provides: "Notre Histoire with scroll storytelling"
      contains: "FadeInUp"
    - path: "src/app/[locale]/infos/page.tsx"
      provides: "Info page with hours, maps, FAQ"
      contains: "openingHours"
    - path: "src/app/[locale]/galerie/page.tsx"
      provides: "Gallery page with lightbox"
      contains: "GalleryLightbox"
    - path: "src/components/gallery/GalleryLightbox.tsx"
      provides: "Lightbox with keyboard nav and swipe"
      contains: "yet-another-react-lightbox"
  key_links:
    - from: "src/app/[locale]/reservation/page.tsx"
      to: "siteSettings.reservationUrl"
      via: "Button href external"
      pattern: "reservationUrl"
    - from: "src/app/[locale]/commander/page.tsx"
      to: "siteSettings.uberEatsUrl"
      via: "external link card"
      pattern: "uberEatsUrl"
    - from: "src/app/[locale]/notre-histoire/page.tsx"
      to: "src/sanity/lib/queries.ts"
      via: "sanityFetch NOTRE_HISTOIRE_QUERY"
      pattern: "NOTRE_HISTOIRE_QUERY"
    - from: "src/components/gallery/GalleryLightbox.tsx"
      to: "yet-another-react-lightbox"
      via: "Lightbox component import"
      pattern: "yet-another-react-lightbox"
    - from: "src/app/[locale]/galerie/page.tsx"
      to: "src/sanity/lib/queries.ts"
      via: "sanityFetch GALLERY_QUERY"
      pattern: "GALLERY_QUERY"
---

<objective>
Build the remaining 5 content pages: Reservation (Gusty CTA + practical info), Commander (3 ordering option cards), Notre Histoire (scroll storytelling with motion animations), Infos (hours, address, maps, FAQ), and Gallery (CSS grid + lightbox).

Purpose: These pages complete the full visitor experience — from discovering the story, to finding practical information, to viewing photos, and ultimately converting via reservation or ordering.
Output: Five fully functional pages with Sanity data, external integrations, scroll animations, and lightbox.
</objective>

<execution_context>
@/home/ekitcho/.claude/get-shit-done/workflows/execute-plan.md
@/home/ekitcho/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-pages/02-RESEARCH.md
@.planning/phases/02-content-pages/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Reservation and Commander pages</name>
  <files>
    src/app/[locale]/reservation/page.tsx
    src/app/[locale]/commander/page.tsx
  </files>
  <action>
    1. Create `src/app/[locale]/reservation/page.tsx`:
       - Async server component: `setRequestLocale(locale)` + `getTranslations({ locale, namespace: 'reservation' })`
       - Fetch: `sanityFetch({ query: SITE_SETTINGS_QUERY, tags: ['siteSettings'] })` for reservationUrl, phone, address, openingHours
       - Section 1 — Hero CTA area:
         - `SectionHeader` with `t('title')` and JP label
         - Large prominent `Button variant="primary" href={settings.reservationUrl} external` with `t('ctaLabel')`
         - Micro-copy below: `p font-body text-sm text-umai-text-muted text-center mt-3` with `t('microCopy')` — "Gratuit, sans commission"
       - Section 2 — Practical Info:
         - `SectionHeader` with `t('practicalInfoTitle')`
         - Two-column grid on desktop (`grid grid-cols-1 md:grid-cols-2 gap-12`)
         - Left column:
           - Opening hours: `h3` with `t('hoursTitle')`, then map `settings.openingHours` rendering day + periods (open-close) as `font-body text-sm`
           - Phone: `h3` with `t('phoneTitle')`, then `<a href="tel:${settings.phone?.replace(/\s/g, '')}">{settings.phone}</a>` with `font-body text-base text-umai-accent`
           - Address: `h3` with `t('addressTitle')`, then `{settings.address?.street}, {settings.address?.postalCode} {settings.address?.city}`
         - Right column:
           - Google Maps embed: `div` with `aspect-[16/9] w-full rounded overflow-hidden`
           - `<iframe>` with Google Maps embed URL for "5 rue des Orphelins, 67000 Strasbourg"
           - Use `loading="lazy"` (native lazy loading, no JS needed per research)
           - `referrerPolicy="no-referrer-when-downgrade"`, `allowFullScreen`, `title="UMAI Ramen — 5 rue des Orphelins, Strasbourg"`
           - Embed URL: `https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d2639.0!2d7.7456!3d48.5808!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x4796c849b5b0c3e1%3A0x0!2s5%20Rue%20des%20Orphelins%2C%2067000%20Strasbourg!5e0!3m2!1sfr!2sfr` (approximate — use Google Maps "Embed a map" for exact URL if possible; this serves as a working fallback)
           - Wrap iframe in `border-0` styling

    2. Create `src/app/[locale]/commander/page.tsx`:
       - Async server component: `setRequestLocale(locale)` + `getTranslations({ locale, namespace: 'commander' })`
       - Fetch: `sanityFetch({ query: SITE_SETTINGS_QUERY, tags: ['siteSettings'] })` for uberEatsUrl, clickCollectUrl, eazeeLinkUrl
       - `SectionHeader` with `t('title')` and JP label
       - Subtitle: `p font-body text-base text-umai-text-muted text-center max-w-lg mx-auto` with `t('subtitle')`
       - Three ordering option cards in `grid grid-cols-1 md:grid-cols-3 gap-6 mt-12`:
         - **Uber Eats card:**
           - `div` with `border border-umai-line p-8 text-center` (or subtle bg-white/50)
           - SVG icon or Uber Eats badge (simple food delivery icon as inline SVG)
           - `h3 font-display text-xl uppercase tracking-wide mt-4` with `t('uberEatsTitle')`
           - `p font-body text-sm text-umai-text-muted mt-2` with `t('uberEatsDesc')` — "Livraison a domicile"
           - `Button variant="primary" href={settings.uberEatsUrl} external` with `t('ctaLabel')`
         - **Click & Collect card:** same structure with `t('clickCollectTitle')`, `t('clickCollectDesc')`, `settings.clickCollectUrl` (may be null/placeholder — handle gracefully with disabled-looking state or placeholder text if URL is empty)
         - **eazee-link card:** same structure with `t('eazeeLinkTitle')`, `t('eazeeLinkDesc')`, `settings.eazeeLinkUrl`
       - Each card: `Button variant="outline"` except Uber Eats which gets `variant="primary"` (per CTA hierarchy, Commander = secondary, but Uber Eats is the primary ordering method)
       - All external links: `target="_blank" rel="noopener noreferrer"` (handled by Button `external` prop)
       - Wrap cards in `FadeInUp` with staggered delays
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `npx next build` succeeds — reservation and commander routes static
    - `/fr/reservation` — shows CTA, micro-copy, hours, phone, address, Google Maps embed
    - `/fr/commander` — shows 3 ordering option cards
    - All external links open in new tabs
    - FadeInUp animations trigger on scroll for commander cards
  </verify>
  <done>
    Reservation page with Gusty CTA and practical info renders correctly. Commander page shows 3 ordering options as cards with external links.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Notre Histoire page with animated scroll storytelling</name>
  <files>
    src/app/[locale]/notre-histoire/page.tsx
  </files>
  <action>
    1. Create `src/app/[locale]/notre-histoire/page.tsx`:
       - Async server component: `setRequestLocale(locale)` + `getTranslations({ locale, namespace: 'histoire' })`
       - Fetch: `sanityFetch({ query: NOTRE_HISTOIRE_QUERY, params: { slug: 'notre-histoire' }, tags: ['page'] })`
       - IMPORTANT: NOTRE_HISTOIRE_QUERY uses `$slug` parameter — pass `params: { slug: 'notre-histoire' }` to sanityFetch. Check if sanityFetch supports params; if not, use the hardcoded slug directly in a specialized query.
       - `SectionHeader` with `t('title')` and JP label
       - Map over `page.sections` (4 sections from Sanity) with alternating layout:
         - Even index (0, 2): photo left, text right — `grid grid-cols-1 md:grid-cols-2 gap-12 items-center`
         - Odd index (1, 3): photo right, text left — same grid but `md:order` classes to swap
         - Photo: `div aspect-[4/5] relative overflow-hidden`, `next/image fill object-cover` using `urlFor(section.image).width(800).auto('format').url()` if image exists
         - Text: `div` with `h2 font-display text-3xl uppercase tracking-wide` using `localized(section.heading, locale)`, `p font-body text-base text-umai-text-muted mt-4 leading-relaxed` using `localized(section.body, locale)`
       - Wrap EACH section in `FadeInUp` with `delay={index * 0.1}` per locked decision (subtle, 0.6s duration, staggered 0.1s)
       - Generous spacing between sections: `space-y-24` or `py-16` per section
       - Final CTA at bottom: `div text-center py-16`, `Button variant="primary" href="/reservation"` with `t('ctaLabel')` — "Reserver une table"
       - Handle empty/missing sections gracefully (fallback text or don't render)
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `npx next build` succeeds — notre-histoire route static
    - `/fr/notre-histoire` — shows sections (may be empty if no Sanity content yet — should handle gracefully)
    - Sections alternate: photo left/text right, then photo right/text left
    - FadeInUp animations trigger on viewport entry for each section
    - Final CTA links to /fr/reservation
  </verify>
  <done>
    Notre Histoire page renders 4 scroll storytelling sections with alternating photo-text layout, Framer Motion fade-in animations, and a reservation CTA at the bottom.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build Infos page, Gallery components with lightbox, and Gallery page</name>
  <files>
    src/app/[locale]/infos/page.tsx
    src/components/gallery/GalleryGrid.tsx
    src/components/gallery/GalleryLightbox.tsx
    src/app/[locale]/galerie/page.tsx
  </files>
  <action>
    1. Create `src/app/[locale]/infos/page.tsx`:
       - Async server component: `setRequestLocale(locale)` + `getTranslations({ locale, namespace: 'infos' })`
       - Fetch: `sanityFetch({ query: INFOS_QUERY, tags: ['siteSettings'] })` for phone, address, openingHours, socialLinks
       - Section 1 — Opening Hours:
         - `SectionHeader` with `t('hoursTitle')`
         - Map over `settings.openingHours` — each entry: `div flex justify-between py-2 border-b border-umai-line last:border-0`
           - `span font-body text-sm font-medium` for day name
           - `span font-body text-sm text-umai-text-muted` for periods (e.g., "12:00 - 14:00, 19:00 - 22:00")
           - Handle closed days (no periods) with "Ferme" text
       - Section 2 — Address + Map:
         - `SectionHeader` with `t('addressTitle')`
         - Address text: `p font-body text-base` with full address
         - Google Maps embed (same pattern as reservation page — extract to a shared inline pattern or duplicate since it is small)
       - Section 3 — Phone:
         - Click-to-call: `<a href="tel:${settings.phone?.replace(/\s/g, '')}">` with `font-body text-lg text-umai-accent hover:underline`
         - `t('phoneTitle')` as label above
       - Section 4 — FAQ (accordion style per Claude's discretion):
         - `SectionHeader` with `t('faqTitle')`
         - Three FAQ items as expandable `<details>` elements (native HTML — no JS needed, accessible by default):
           - `<summary>` with `font-body text-sm font-medium cursor-pointer py-3 border-b border-umai-line` — question text from i18n
           - `<p>` inside details with `font-body text-sm text-umai-text-muted py-3 pl-4` — answer text from i18n
           - FAQ 1: `t('faqAllergens')` / `t('faqAllergensAnswer')` — allergen info
           - FAQ 2: `t('faqGroups')` / `t('faqGroupsAnswer')` — group reservations
           - FAQ 3: `t('faqVegetarian')` / `t('faqVegetarianAnswer')` — vegetarian options
       - Section 5 — Contact/Social:
         - Instagram link + Facebook link from `settings.socialLinks`
         - Use inline SVG icons (Instagram, Facebook) with `text-umai-text-muted hover:text-umai-accent transition-colors`
         - Each as `<a>` with `target="_blank" rel="noopener noreferrer"`

    2. Create `src/components/gallery/GalleryGrid.tsx` — server component for the CSS grid:
       - Props: `photos` array, `locale: string`
       - `div` with `grid grid-cols-2 md:grid-cols-3 gap-3`
       - Each photo: `button` (for lightbox trigger, handled by parent) with `aspect-[4/3] relative overflow-hidden group`
         - `next/image fill object-cover` with `urlFor(photo.image.asset).width(600).auto('format').url()`
         - `sizes="(max-width: 768px) 50vw, 33vw"`
         - Hover effect: `transition-transform duration-500 group-hover:scale-105` on the image
       - Actually, since the lightbox needs client-side state, combine GalleryGrid and GalleryLightbox into one client component.

    3. Create `src/components/gallery/GalleryLightbox.tsx` — `'use client'` component:
       - Combines grid + lightbox in one client component
       - Props: `photos` array from GALLERY_QUERY (with image.asset.url, metadata.dimensions), `locale: string`
       - State: `open: boolean`, `index: number`
       - Grid rendering: same as GalleryGrid pattern above, but each photo is a `<button>` that sets `setIndex(i); setOpen(true)`
       - Slides: map photos to `{ src: photo.image.asset.url + '?auto=format&w=1600', width: photo.image.asset.metadata.dimensions.width, height: photo.image.asset.metadata.dimensions.height, alt: localized(photo.alt, locale) }`
       - Import `Lightbox` from `yet-another-react-lightbox` and `'yet-another-react-lightbox/styles.css'`
       - Render `<Lightbox open={open} close={() => setOpen(false)} index={index} slides={slides} />` — use default renderer (no custom NextJsImage renderer needed for v1 — the default handles keyboard and swipe; custom renderer is a nice-to-have optimization)
       - Keyboard nav (arrow keys, Escape) and mobile swipe are built into yet-another-react-lightbox by default
       - Use `urlFor` for thumbnail images in the grid, but direct Sanity CDN URL (from asset.url) for lightbox slides (higher quality, larger size)

    4. Create `src/app/[locale]/galerie/page.tsx`:
       - Async server component: `setRequestLocale(locale)` + `getTranslations({ locale, namespace: 'gallery' })`
       - Fetch: `sanityFetch({ query: GALLERY_QUERY, tags: ['gallery'] })`
       - `SectionHeader` with `t('title')` and JP label
       - `<GalleryLightbox photos={photos} locale={locale} />`
       - Handle empty gallery gracefully (message or don't render grid)
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `npx next build` succeeds — all routes static
    - `/fr/infos` — shows hours, address, map, phone (click-to-call), FAQ, social links
    - `/fr/galerie` — shows photo grid (may be empty without Sanity content)
    - FAQ uses native `<details>` elements that expand on click
    - Phone number is a clickable tel: link
    - Google Maps embed lazy-loads (check network tab)
    - Lightbox opens on photo click, closes on Escape, navigates with arrow keys
  </verify>
  <done>
    Infos page displays structured hours, address, Google Maps, phone, FAQ (accordion), and social links. Gallery page shows responsive CSS grid with clickable lightbox supporting keyboard navigation and mobile swipe. All five remaining pages are complete.
  </done>
</task>

</tasks>

<verification>
- `npx next build` — all 7 page routes static for all 3 locales
- Reservation page: Gusty CTA, micro-copy, hours, phone, address, Google Maps embed
- Commander page: 3 ordering option cards with external links
- Notre Histoire: 4 sections with alternating layout and scroll animations
- Infos: hours table, address, Google Maps, click-to-call phone, FAQ, social links
- Gallery: CSS grid, lightbox opens on click, keyboard + swipe navigation
- All pages render correctly in /fr, /en, /de
</verification>

<success_criteria>
All five remaining content pages are fully functional. Visitors can find reservation info, discover ordering options, read the story with animated sections, access practical info with FAQ, and browse photos in a lightbox. All external integrations (Gusty, Uber Eats, eazee-link, Google Maps) are wired.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-pages/02-04-SUMMARY.md`
</output>
