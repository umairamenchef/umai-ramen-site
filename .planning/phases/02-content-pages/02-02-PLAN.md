---
phase: 02-content-pages
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/[locale]/page.tsx
  - src/components/home/Hero.tsx
  - src/components/home/MenuPreview.tsx
  - src/components/home/UspSection.tsx
  - src/components/home/HistoireTeaser.tsx
  - src/components/home/GalleryPreview.tsx
  - src/components/home/SocialSection.tsx
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - HOME-05
  - HOME-06
  - HOME-07
  - INTG-01
  - INTG-02
  - INTG-06

must_haves:
  truths:
    - "Visitor sees fullscreen hero with photo, catchphrase, and two CTA buttons"
    - "Reserve CTA links to Gusty reservation URL from Sanity"
    - "Commander CTA links to Uber Eats URL from Sanity"
    - "Three menu preview cards with photo backgrounds link to /menu"
    - "Three USP blocks display in dotted border cards with line-art icons"
    - "Notre Histoire teaser shows photo + text + CTA to /notre-histoire"
    - "Gallery section shows 4-6 thumbnails with link to /galerie"
    - "Social section shows Instagram handle and branded hashtag"
  artifacts:
    - path: "src/app/[locale]/page.tsx"
      provides: "Homepage route with Sanity data fetching"
      contains: "HOMEPAGE_QUERY"
    - path: "src/components/home/Hero.tsx"
      provides: "Fullscreen hero with next/image fill, overlay, catchphrase, CTAs"
      contains: "preload"
    - path: "src/components/home/MenuPreview.tsx"
      provides: "3 category cards with photo backgrounds"
      contains: "menuCategories"
    - path: "src/components/home/UspSection.tsx"
      provides: "3 proof blocks with dotted borders"
      contains: "border-dashed"
    - path: "src/components/home/HistoireTeaser.tsx"
      provides: "Photo + text + CTA to /notre-histoire"
      contains: "notre-histoire"
    - path: "src/components/home/GalleryPreview.tsx"
      provides: "Photo grid preview with link to /galerie"
      contains: "galerie"
    - path: "src/components/home/SocialSection.tsx"
      provides: "Instagram link and hashtag"
      contains: "instagram"
  key_links:
    - from: "src/app/[locale]/page.tsx"
      to: "src/sanity/lib/queries.ts"
      via: "sanityFetch with HOMEPAGE_QUERY"
      pattern: "HOMEPAGE_QUERY"
    - from: "src/components/home/Hero.tsx"
      to: "siteSettings.reservationUrl"
      via: "Button href prop"
      pattern: "reservationUrl"
    - from: "src/components/home/MenuPreview.tsx"
      to: "/menu"
      via: "Link with hash anchor"
      pattern: "menu.*slug"
---

<objective>
Build the complete homepage (Accueil) with fullscreen hero, menu preview cards, USP blocks, Notre Histoire teaser, gallery preview grid, and social section — all pulling data from Sanity via the HOMEPAGE_QUERY.

Purpose: The homepage is the primary landing page and must demonstrate the full UMAI brand identity with clear conversion paths (reserve/order CTAs).
Output: A fully functional homepage at /[locale]/ with Sanity-driven content and all 7 sections.
</objective>

<execution_context>
@/home/ekitcho/.claude/get-shit-done/workflows/execute-plan.md
@/home/ekitcho/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-content-pages/02-RESEARCH.md
@.planning/phases/02-content-pages/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Hero, MenuPreview, and UspSection components</name>
  <files>
    src/components/home/Hero.tsx
    src/components/home/MenuPreview.tsx
    src/components/home/UspSection.tsx
  </files>
  <action>
    1. Create `src/components/home/Hero.tsx` — Fullscreen hero section:
       - `section` with `relative h-screen min-h-[600px] flex items-center justify-center`
       - Background: `next/image` with `fill`, `preload` (NOT `priority` — deprecated in Next.js 16), `loading="eager"`, `className="object-cover object-center"`, `sizes="100vw"`. Use a placeholder hero image path (e.g., `/photos/hero.jpg`) — the owner will update via Sanity later. For now use a div with bg-umai-black as fallback if no image.
       - Overlay: `div` with `absolute inset-0 bg-black/45` and `aria-hidden="true"`
       - Content: `relative z-10 text-center px-6 max-w-3xl`
       - Catchphrase: `h1` with `font-display text-4xl sm:text-5xl md:text-7xl uppercase tracking-[0.1em] text-white` and `style={{ textShadow: '0 2px 12px rgba(0,0,0,0.4)' }}`. Display catchphrase from props (Sanity localeString field), falling back to "Nouilles fraiches. Bouillons maison."
       - CTAs: flex container `flex flex-col sm:flex-row gap-4 justify-center mt-8`
         - "Reserver" button: `Button variant="primary" href={reservationUrl} external` — per locked CTA hierarchy, primary vert accent fill
         - "Commander" button: `Button variant="outline-white" href={uberEatsUrl} external` — per locked decision, outlined white on hero
       - Props: `catchphrase: string`, `reservationUrl: string`, `uberEatsUrl: string`, `heroImage?: SanityImageSource` (optional for when Sanity has the image)
       - Render translated CTA labels from `getTranslations` passed as props or use `useTranslations('common')` in a client wrapper. PREFER: pass translated strings as props to keep Hero as a server component.

    2. Create `src/components/home/MenuPreview.tsx` — 3 category cards:
       - Section with `SectionHeader` (title from i18n `home.menuPreviewTitle`, JP label optional)
       - `div` with `grid grid-cols-1 md:grid-cols-3 gap-6`
       - Each card: `Link` (from `@/i18n/navigation`) to `/menu` with `scroll={false}` — hash anchors don't work well with next-intl Link, so just link to /menu page
       - Card: `relative aspect-[4/3] overflow-hidden group` container
         - `next/image` with `fill`, `object-cover`, `sizes="(max-width: 768px) 100vw, 33vw"`, source from `urlFor(category.image).width(600).auto('format').url()` if image exists, otherwise a colored div placeholder
         - Dark overlay: `absolute inset-0 bg-black/30 group-hover:bg-black/40 transition-colors`
         - Category name: `absolute bottom-0 left-0 right-0 p-6`, `font-display text-2xl text-white uppercase tracking-wide`
       - Props: `categories` array with `_id, name, slug, image`, `locale: string`
       - Use `localized(name, locale)` for category name display
       - CTA link below grid: "Voir tout le menu" linking to /menu

    3. Create `src/components/home/UspSection.tsx` — 3 proof blocks:
       - Section with `SectionHeader` (title from i18n `home.uspTitle`)
       - `div` with `grid grid-cols-1 md:grid-cols-3 gap-8`
       - Each USP block: `div` with `border border-dashed border-umai-line p-8 text-center`
         - Line-art SVG icon above (simple inline SVGs: a noodle bowl for nouilles, a pot/steam for bouillons, a map pin for local — keep SVGs minimal, ~20-30 lines each)
         - `h3` with `font-display text-xl uppercase tracking-wide mt-4`
         - `p` with `font-body text-sm text-umai-text-muted mt-2`
       - Content from i18n: `home.uspNouilles`, `home.uspBouillons`, `home.uspLocal` (titles) + `home.uspNouillesDesc`, `home.uspBouillonsDesc`, `home.uspLocalDesc` (descriptions)
       - Wrap each block in `FadeInUp` with staggered delay (0, 0.1, 0.2)
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - All three component files exist and export named functions
    - Hero.tsx uses `preload` (not `priority`) on the Image component
    - MenuPreview.tsx uses `urlFor` for image sources
    - UspSection.tsx uses `FadeInUp` for scroll animations
  </verify>
  <done>
    Hero, MenuPreview, and UspSection components are ready for assembly. Hero shows fullscreen image with catchphrase and CTAs. MenuPreview shows 3 category cards. UspSection shows 3 dotted-border proof blocks with fade-in animations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build HistoireTeaser, GalleryPreview, SocialSection and assemble the homepage</name>
  <files>
    src/components/home/HistoireTeaser.tsx
    src/components/home/GalleryPreview.tsx
    src/components/home/SocialSection.tsx
    src/app/[locale]/page.tsx
  </files>
  <action>
    1. Create `src/components/home/HistoireTeaser.tsx` — Notre Histoire teaser:
       - Horizontal split: `grid grid-cols-1 md:grid-cols-2 gap-12 items-center`
       - Left (desktop): photo with `next/image`, `aspect-[4/5]`, `object-cover`, placeholder image from Sanity or local
       - Right: `font-display text-3xl uppercase tracking-wide` heading, `font-body text-base text-umai-text-muted mt-4` teaser text from i18n `home.histoireTeaser`, `Button variant="outline" href="/notre-histoire"` CTA from i18n `home.histoireCta`
       - Wrap in `FadeInUp`
       - Props: `locale: string` (teaser is from i18n, not Sanity — it is a fixed promotional blurb)

    2. Create `src/components/home/GalleryPreview.tsx` — photo preview grid:
       - Section with `SectionHeader`
       - `div` with `grid grid-cols-2 md:grid-cols-3 gap-3`
       - Each photo: `div` with `aspect-[4/3] relative overflow-hidden`, `next/image fill object-cover`, source from `urlFor(photo.image).width(400).auto('format').url()`
       - Max 6 photos displayed
       - Below grid: `Link` to `/galerie` with text from i18n `home.galleryCta`, styled as text link with underline
       - Props: `photos` array (from HOMEPAGE_QUERY galleryPreview), `locale: string`

    3. Create `src/components/home/SocialSection.tsx` — Instagram link:
       - Centered section with `py-16 text-center`
       - Instagram SVG icon (simple inline, 24x24)
       - `p` with `font-body text-sm uppercase tracking-widest text-umai-text-muted` showing `@umai_ramen_strasbourg`
       - `p` with hashtag from i18n `home.socialHashtag` (e.g. `#umairamen`)
       - Link wrapping the handle, pointing to `socialLinks.instagram` URL, `target="_blank" rel="noopener noreferrer"`
       - Props: `instagramUrl: string`

    4. Rewrite `src/app/[locale]/page.tsx` — complete homepage assembly:
       - Async server component with `setRequestLocale(locale)` pattern (already exists)
       - Fetch data with `sanityFetch({ query: HOMEPAGE_QUERY, tags: ['siteSettings', 'menuCategory', 'menuItem', 'gallery'] })`
       - Use `getTranslations({ locale, namespace: 'home' })` and `getTranslations({ locale, namespace: 'common' })` for server-side translations
       - Pass translated strings as props to components (keeps them as server components where possible)
       - Render sections in order with generous spacing (`py-20` or `py-[var(--spacing-section)]`):
         1. `<Hero>` — full width, outside max-width container
         2. `<MenuPreview>` — inside max-width container
         3. `<UspSection>` — inside max-width container
         4. `<HistoireTeaser>` — inside max-width container
         5. `<GalleryPreview>` — inside max-width container
         6. `<SocialSection>` — inside max-width container
       - Extract `settings`, `menuCategories`, `galleryPreview` from query result
       - Use `localized(settings.catchphrase, locale)` for hero catchphrase
       - Provide sensible fallback URLs for reservation/uberEats (e.g., '#' if not set in Sanity)
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `npx next build` succeeds — homepage route is static (ISR)
    - Visit `http://localhost:3000/fr` — all 7 sections render (hero, menu preview, USP, histoire teaser, gallery, social)
    - Visit `http://localhost:3000/en` and `/de` — same structure with locale-appropriate text
    - Hero reserve/order buttons have correct external link attributes (target, rel)
  </verify>
  <done>
    Complete homepage renders all 7 sections at /[locale]/ with Sanity data (catchphrase, categories, gallery) and i18n text. CTAs link to external booking/ordering services. Responsive layout works at all breakpoints.
  </done>
</task>

</tasks>

<verification>
- `npx next build` — all routes static, build succeeds
- Homepage renders 7 sections in correct order: Hero > Menu Preview > USP > Histoire Teaser > Gallery > Social
- Hero CTAs open external URLs in new tab
- Menu preview cards link to /menu
- Notre Histoire teaser CTA links to /notre-histoire
- Gallery preview links to /galerie
- All text renders correctly in /fr, /en, /de
</verification>

<success_criteria>
The homepage is fully functional at all three locale URLs. A visitor sees the fullscreen hero with catchphrase and can reach reservation (Gusty) and ordering (Uber Eats) via one click. All homepage sections display correctly with Sanity-driven content where applicable and i18n static text elsewhere.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-pages/02-02-SUMMARY.md`
</output>
