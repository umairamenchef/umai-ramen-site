---
phase: 04-ingestion-classification
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - ig-studio/package.json
  - ig-studio/src/contact-sheet.js
autonomous: false
requirements:
  - INGEST-07
  - INGEST-08

must_haves:
  truths:
    - "`npm run contact-sheet` reads classification.json and writes ig-studio/contact-sheet.html"
    - "Opening contact-sheet.html in a browser shows one card per photo: thumbnail + proposed dishSlug + shotType + confidence + reasoning"
    - "Cards are sortable by confidence so the human reviews lowest-confidence (most error-prone) photos first"
    - "After human review + corrections + sign-off, `npm run classify -- --all` is unblocked"
  artifacts:
    - path: "ig-studio/src/contact-sheet.js"
      provides: "Generates a self-contained HTML contact sheet from classification.json (thumbnails via relative photo paths)"
      contains: "classification.json"
  key_links:
    - from: "ig-studio/src/contact-sheet.js"
      to: "ig-studio/classification.json"
      via: "reads the classification array produced by 04-01"
      pattern: "classification.json"
    - from: "ig-studio/contact-sheet.html"
      to: "ig-studio/photos/"
      via: "img src relative paths to the local photos"
      pattern: "photos/"
---

<objective>
Add the human-review surface that closes Phase 04: an HTML contact sheet generated from `classification.json` (thumbnails + proposed label + confidence + reasoning, sortable by confidence), plus the pilot validation checkpoint where the owner reviews the pilot, applies corrections, and signs off — unblocking the full 81-photo `--all` run.

Purpose: Classification errors are expected; nothing downstream should run until a human has eyeballed the pilot. This plan turns the raw `classification.json` into something reviewable in seconds and gates industrialization on that review.
Output: `ig-studio/contact-sheet.html` generator + a signed-off pilot.
</objective>

<execution_context>
@/Users/ekitcho/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ekitcho/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-ingestion-classification/04-CONTEXT.md
@.planning/phases/04-ingestion-classification/04-01-PLAN.md
@ig-studio/.gitignore

<interfaces>
From 04-01 — classification.json is an array of:
```json
{ "file": "umai_007.jpg", "dishSlug": "tokyo", "shotType": "packshot", "confidence": 0.82, "reasoning": "...", "override": false }
```
- The pilot gate in `ig-studio/src/cli.js` checks for `ig-studio/.pilot-signoff`; `npm run signoff` creates it.
- contact-sheet.html and classification.json are gitignored (added in 04-01). photos/ is gitignored — reference photos by relative path, do NOT embed/commit them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HTML contact-sheet generator (thumbnails + label + confidence, sortable)</name>
  <files>
    ig-studio/src/contact-sheet.js
    ig-studio/package.json
  </files>
  <action>
1. **ig-studio/src/contact-sheet.js**:
   - Read `classification.json` (package root); error clearly with "Run `npm run classify -- --pilot` first" if missing/empty.
   - Generate a single self-contained `contact-sheet.html` (inline CSS + a few lines of inline JS, no build step) at the package root with one card per entry:
     - Thumbnail: `img src="photos/<file>" loading="lazy"` (relative path — works when the HTML is opened from `ig-studio/`). Constrain to a small thumbnail (e.g. 220px) via CSS.
     - Label line: `dishSlug` (large) + a `shotType` badge.
     - Confidence: numeric + a colored badge/bar (red < 0.5, amber 0.5–0.7, green >= 0.7) so low-confidence photos pop.
     - `reasoning` (small text).
     - An `override` indicator when `override` is true (corrected entries visually distinct).
   - Default render order: confidence ASCENDING (worst first = review priority). Add minimal inline JS buttons to re-sort cards by confidence asc/desc and by dishSlug (client-side). (INGEST-07: sortable.)
   - Escape any text injected into HTML (reasoning) to avoid broken markup.
   - Print the output path and a count summary to stdout.
2. **ig-studio/package.json**: add script `"contact-sheet": "node src/contact-sheet.js"` (no --env-file — it makes no API calls).
  </action>
  <verify>
    <automated>cd ig-studio && node scripts/_verify-contact-sheet.mjs</automated>
  </verify>
  <done>contact-sheet.js reads classification.json and emits a self-contained contact-sheet.html with one card per entry (thumbnail via relative photos/ path, dishSlug, shotType, confidence badge, reasoning), confidence-sorted and client-side re-sortable. Generator runs without an API call.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Pilot review + sign-off gate (INGEST-08)</name>
  <action>This is a human checkpoint, not a code task. Claude first runs the pilot end-to-end (`npm run classify -- --pilot` then `npm run contact-sheet`) to produce classification.json + contact-sheet.html, then pauses for the owner to review labels, apply corrections (set override:true), and sign off via `npm run signoff`. The sign-off marker (.pilot-signoff) is what unblocks the 81-photo `--all` run in 04-01 (INGEST-08).</action>
  <what-built>
The full pilot path is automated and ready for human review:
- `npm run classify -- --pilot` classified ~10 varied local photos into classification.json (04-01).
- `npm run contact-sheet` rendered contact-sheet.html (thumbnails + proposed labels + confidence, sorted worst-first).
  </what-built>
  <how-to-verify>
1. From `ig-studio/`, run `npm run classify -- --pilot` (uses the live ANTHROPIC_API_KEY in .env), then `npm run contact-sheet`.
2. Open `ig-studio/contact-sheet.html` in a browser. For each of the ~10 cards, confirm the proposed dishSlug/shotType is plausible against the photo, paying closest attention to the low-confidence (red/amber) cards shown first.
3. For any wrong label: edit the entry in `ig-studio/classification.json` — fix `dishSlug`/`shotType` and set `"override": true` on that entry. Re-run `npm run contact-sheet` to confirm the correction shows (override indicator + updated label).
4. Verify the broth disambiguation behaved sanely on examples present in the pilot: clear broth + pork → tokyo; clear broth + chicken + citrus → yuzu; reddish-sesame + minced beef → tantan; cold separated noodles + dip bowl → tsukemen; no broth → mazesoba; cold dressed noodles → hiyashi; interior/people/multi-dish → ambiance.
5. When satisfied, run `npm run signoff` to create `ig-studio/.pilot-signoff`, then confirm the gate opens: `npm run classify -- --all` no longer exits with the "Pilot not signed off" error (you may Ctrl-C the full run once it starts — running all 81 is optional here and consumes API budget).
  </how-to-verify>
  <resume-signal>Type "approved" once the pilot labels look right, corrections are saved with override:true, and `npm run signoff` has unblocked --all. Or describe the labels that came out wrong so the classifier prompt (04-01 kb.js) can be tuned.</resume-signal>
</task>

</tasks>

<verification>
1. `cd ig-studio && node scripts/_verify-contact-sheet.mjs` passes (generator emits valid HTML from a fixture).
2. contact-sheet.html opens in a browser and shows thumbnails + labels + confidence for every classification.json entry, sortable by confidence.
3. Human pilot review completed; wrong labels corrected in classification.json with override:true; correction visible after re-running contact-sheet.
4. `npm run signoff` created `.pilot-signoff`; `npm run classify -- --all` no longer blocks.
5. No photos, .env, classification.json, contact-sheet.html, or .pilot-signoff staged for commit (all gitignored).
</verification>

<success_criteria>
- INGEST-07: HTML contact sheet renders thumbnail + classification + confidence for the pilot batch, sortable for fast review.
- INGEST-08: Pilot validation gate enforced end to end — full 81-photo `--all` run stays blocked until the owner reviews the contact sheet, applies corrections, and signs off.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ingestion-classification/04-02-SUMMARY.md`.
Suggested atomic commits:
- `feat(04-02): HTML contact-sheet generator from classification.json (INGEST-07)`
- (Task 2 is a human checkpoint — no code commit; record sign-off in the SUMMARY.)

Note for the executor: the verify command references `ig-studio/scripts/_verify-contact-sheet.mjs` — create that small fixture script (writes a 2-entry classification.json, imports contact-sheet.js, asserts the HTML contains the file name, dishSlug, confidence, `photos/`, and an `ambiance`/override marker, then cleans up). Keep it out of the committed runtime path or commit it under `scripts/` (it is a test helper, safe to commit).
</output>
