---
phase: 07-ig-studio-ui
plan: 05
type: execute
wave: 4
depends_on: ["07-04"]
files_modified:
  - src/app/api/ig-studio/export/route.ts
  - src/app/(ig-studio)/ig-studio/page.tsx
requirements:
  - REVIEW-05
  - CAPTION-04
autonomous: true
must_haves:
  truths:
    - "GET /api/ig-studio/export (authed) returns a .zip of all VALIDATED (override:true) posts; unauthed → 401"
    - "Each validated post folder in the zip contains feed.png, square.png, story.png + caption.txt (caption.txt assembled from captions.json)"
    - "Photos with composed out/{id} but missing variants are skipped with a note (export never crashes); a manifest lists what was included/skipped"
    - "An 'Exporter les posts validés' control on /ig-studio triggers the download"
  artifacts:
    - path: "src/app/api/ig-studio/export/route.ts"
      provides: "Auth-gated zip export of validated out/{id} trees + caption.txt + manifest"
      contains: "requireStudioAuth"
    - path: "src/app/(ig-studio)/ig-studio/page.tsx"
      provides: "Export button/link to /api/ig-studio/export"
      contains: "/api/ig-studio/export"
  key_links:
    - from: "src/app/api/ig-studio/export/route.ts"
      to: "ig-studio/out"
      via: "reads validated out/{id}/*.png"
      pattern: "out"
    - from: "src/app/api/ig-studio/export/route.ts"
      to: "ig-studio/captions.json"
      via: "writes caption.txt per post from the caption store"
      pattern: "captions"
---

<objective>
Close the loop: let Loan download all finalized posts in one click. An auth-gated export route gathers every VALIDATED photo (override:true), assembles each `out/{id}/` (feed/square/story PNGs) plus a `caption.txt` built from captions.json, and streams a single `.zip`. This produces the clean per-post deliverable tree the owner posts manually (bridges CAPTION-04's `out/{id}/` assembly, UI-driven).

Purpose: The editor finalizes posts one by one; this gives a batch deliverable so the work leaves the tool. It is the "export validated posts" success criterion from 07-CONTEXT.
Output: the export API route + an export control on the gallery.

NOTE: zipping uses the macOS built-in `zip` via shell-out (consistent with the project's macOS-only `sips` dependency for image ops) — no new npm dependency. Deployment-for-Loan (online) is later; local-dev export is sufficient for v1.
</objective>

<execution_context>
@/Users/ekitcho/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ekitcho/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ig-studio-ui/07-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-ig-studio-ui/07-04-PLAN.md

<environment>
- Next 16. Route handler self-guards with `await requireStudioAuth()` (→401). `export const dynamic = 'force-dynamic';`.
- Stores read from `ig-studio/` at runtime via process.cwd() (see 07-03 data.ts): `readClassification()`, `readCaptions()`, `IG_ROOT`, out path.
- "Validated" = classification entry with `override === true`. Only those are exported.
- Build the export under `os.tmpdir()`: stage `export/{id}/` with the 3 PNGs (copied from `ig-studio/out/{id}/`) + `caption.txt` (from captions.json, or the dish baseline as a fallback) + a top-level `manifest.json` (included ids, skipped ids + reason). Then `zip -r umai-posts.zip .` via child_process from the staging dir (macOS built-in). Read the zip, clean up temp, stream it with `Content-Type: application/zip` + `Content-Disposition: attachment; filename="umai-posts.zip"`.
- A photo marked validated but with no `out/{id}/feed.png` yet → skip + record in manifest (it just needs a Regenerate). Never throw on a single bad entry.
- Depends on 07-04 (validated overrides + regenerated out/ trees exist) and transitively 07-03 (data lib) + 07-02 (compose produces out/).
</environment>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export zip route + gallery export control</name>
  <files>
    src/app/api/ig-studio/export/route.ts
    src/app/(ig-studio)/ig-studio/page.tsx
  </files>
  <action>
1. **src/app/api/ig-studio/export/route.ts**:
   - `export const dynamic = 'force-dynamic';`
   - `GET(req)`: `await requireStudioAuth()` (→401 on throw).
   - `const entries = readClassification().filter(e => e.override === true);`
   - `const captions = readCaptions();`
   - Create staging dir `mkdtemp(join(tmpdir(),'umai-export-'))`. For each validated entry: `id = file.replace(/\.jpg$/,'')`; source `resolve(IG_ROOT,'out',id)`. If `out/{id}/feed.png` missing → push `{id, reason:'not composed'}` to skipped, continue. Else `mkdir export/{id}`, copy `feed.png`/`square.png`/`story.png` that exist, write `caption.txt` = `captions[id]?.text ?? (entry.baseline || entry.dishName || '')`. Record included id.
   - Write `manifest.json` `{ exportedAt, included:[...], skipped:[...] }` at staging root.
   - `execFileSync('zip', ['-r','umai-posts.zip','.'], { cwd: stagingDir })` (or spawn). Read `umai-posts.zip` into a buffer. `rm -rf` the staging dir.
   - Return the buffer with `Content-Type: application/zip` and `Content-Disposition: attachment; filename="umai-posts.zip"`. If `included.length === 0` → still return a zip containing only manifest.json (so the user sees why nothing was exported), or a 200 JSON `{ included:0, skipped }` — pick the zip-with-manifest path for consistency.
2. **src/app/(ig-studio)/ig-studio/page.tsx** — add an "Exporter les posts validés" button in the header: a plain `<a href="/api/ig-studio/export">` (download). Show the validated count next to it.
  </action>
  <verify>
    <automated>cd /Users/ekitcho/Desktop/dev-claude-lab/umai2026 && npm run dev >/tmp/igdev5.log 2>&1 & DEV=$!; for i in $(seq 1 30); do curl -sf -o /dev/null http://localhost:3000/ig-studio/login && break; sleep 2; done; TOK=$(node -e "globalThis.crypto.subtle.digest('SHA-256',new TextEncoder().encode(process.env.IG_STUDIO_PASSWORD||'')).then(b=>process.stdout.write([...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,'0')).join('')))"); NA=$(curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/api/ig-studio/export); CODE=$(curl -s -o /tmp/umai-posts.zip -w '%{http_code}:%{content_type}' --cookie "ig_studio_auth=$TOK" http://localhost:3000/api/ig-studio/export); H=$(curl -s --cookie "ig_studio_auth=$TOK" http://localhost:3000/ig-studio); kill $DEV; echo "noauth=$NA export=$CODE"; [ "$NA" = "401" ] && echo "$CODE" | grep -q '200:application/zip' && unzip -l /tmp/umai-posts.zip | grep -q 'manifest.json' && echo "$H" | grep -q '/api/ig-studio/export' && echo PASS || (echo FAIL && exit 1)</automated>
  </verify>
  <done>GET /api/ig-studio/export returns a valid application/zip (containing manifest.json + each validated post's feed/square/story.png + caption.txt) when authed, 401 unauthed; entries lacking composed output are skipped and listed in the manifest; the gallery header has an export download control showing the validated count. Manually verified: after validating + regenerating a couple of photos, the zip contains those post folders with caption.txt.</done>
</task>

</tasks>

<verification>
1. Task 1 verify: 401 unauthed; 200 application/zip authed; zip lists manifest.json; gallery links to the export route.
2. Manual: validate + Regenerate 2 photos in the editor, then download the zip → contains `{id}/feed.png|square.png|story.png|caption.txt` for those ids and a manifest with included/skipped.
3. No zip / out/ / captions.json artifacts staged for commit; only the route + page change committed.
</verification>

<success_criteria>
- REVIEW-05 (output side): finalized per-photo deliverables (regenerated images) are collected and leave the tool as a single download.
- CAPTION-04 (UI-assembled): each exported post folder is the clean `{id}/feed.png, square.png, story.png, caption.txt` tree — assembled at export from the editor's regenerated images + saved captions. (Batch `npm run ig:build:all` over all 81, CAPTION-06, remains an optional Phase 06 pass.)
</success_criteria>

<output>
After completion, create `.planning/phases/07-ig-studio-ui/07-05-SUMMARY.md`.
Suggested atomic commit:
- `feat(07-05): export validated posts as a zip (out tree + caption.txt + manifest) (REVIEW-05, CAPTION-04 UI)`
</output>
